type id = integer;

type user = struct "User" {
    /// unique id of the user
    id: id,
    name: string?,
    type: enum {"admin", "writer", "reader"},
    SNSs: [string]
};

type group = struct "Group" {
  id: id,
  members: [user]
};
